---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gpgkeys.forgejo.forgejo.m.crossplane.io
spec:
  group: forgejo.forgejo.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - forgejo
    kind: GpgKey
    listKind: GpgKeyList
    plural: gpgkeys
    singular: gpgkey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GpgKey is the Schema for the GpgKeys API. Forgejo user GPG key
          resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GpgKeySpec defines the desired state of GpgKey
            properties:
              forProvider:
                properties:
                  armoredPublicKey:
                    description: |-
                      (String) Armored GPG public key. Changing this forces a new resource to be created.
                      Armored GPG public key. Changing this forces a new resource to be created.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  armoredPublicKey:
                    description: |-
                      (String) Armored GPG public key. Changing this forces a new resource to be created.
                      Armored GPG public key. Changing this forces a new resource to be created.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.armoredPublicKey is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.armoredPublicKey)
                || (has(self.initProvider) && has(self.initProvider.armoredPublicKey))'
          status:
            description: GpgKeyStatus defines the observed state of GpgKey.
            properties:
              atProvider:
                properties:
                  armoredPublicKey:
                    description: |-
                      (String) Armored GPG public key. Changing this forces a new resource to be created.
                      Armored GPG public key. Changing this forces a new resource to be created.
                    type: string
                  canCertify:
                    description: |-
                      (Boolean) Can this key certify.
                      Can this key certify.
                    type: boolean
                  canEncryptComms:
                    description: |-
                      (Boolean) Can this key encrypt communications.
                      Can this key encrypt communications.
                    type: boolean
                  canEncryptStorage:
                    description: |-
                      (Boolean) Can this key encrypt storage.
                      Can this key encrypt storage.
                    type: boolean
                  canSign:
                    description: |-
                      (Boolean) Can this key sign.
                      Can this key sign.
                    type: boolean
                  createdAt:
                    description: |-
                      (String) Time at which the GPG key was created.
                      Time at which the GPG key was created.
                    type: string
                  emails:
                    description: |-
                      (List of Object) Emails associated with the GPG key. (see below for nested schema)
                      Emails associated with the GPG key.
                    items:
                      properties:
                        email:
                          description: (String)
                          type: string
                        verified:
                          description: (Boolean)
                          type: boolean
                      type: object
                    type: array
                  expiresAt:
                    description: |-
                      (String) Time at which the GPG key expires.
                      Time at which the GPG key expires.
                    type: string
                  id:
                    description: (Number) Numeric identifier of the GPG key.
                    type: string
                  keyId:
                    description: |-
                      (String) ID of the GPG key.
                      ID of the GPG key.
                    type: string
                  primaryKeyId:
                    description: |-
                      (String) Primary ID of the GPG key.
                      Primary ID of the GPG key.
                    type: string
                  publicKey:
                    description: |-
                      (String) The public key.
                      The public key.
                    type: string
                  subkeys:
                    description: |-
                      (List of Object) Subkeys of the GPG key. (see below for nested schema)
                      Subkeys of the GPG key.
                    items:
                      properties:
                        canCertify:
                          description: (Boolean) Can this key certify.
                          type: boolean
                        canEncryptComms:
                          description: (Boolean) Can this key encrypt communications.
                          type: boolean
                        canEncryptStorage:
                          description: (Boolean) Can this key encrypt storage.
                          type: boolean
                        canSign:
                          description: (Boolean) Can this key sign.
                          type: boolean
                        createdAt:
                          description: (String) Time at which the GPG key was created.
                          type: string
                        expiresAt:
                          description: (String) Time at which the GPG key expires.
                          type: string
                        id:
                          description: (Number) Numeric identifier of the GPG key.
                          type: number
                        keyId:
                          description: (String) ID of the GPG key.
                          type: string
                        primaryKeyId:
                          description: (String) Primary ID of the GPG key.
                          type: string
                        publicKey:
                          description: (String) The public key.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
